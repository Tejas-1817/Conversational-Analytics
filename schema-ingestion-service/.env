# Copy to .env and fill in. Never commit .env.

METADATA_DB_URL=postgresql+psycopg://ingestion:ingestion@localhost:5432/metadata
REDIS_URL=redis://localhost:6379/0

# API auth (skeleton-level; replace with SSO/OIDC before any real deployment)
API_KEY="ci2oPj_RAuME8jk6r1_N4xPNAiF1Z_fy"

# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=ivbXJ7fYRsloNvFosOTYsuXUVObAA4twxf13qEhbCUM=

# Profiling guardrails
PROFILE_SAMPLE_ROWS=10000
PROFILE_TOP_N_VALUES=20
STATEMENT_TIMEOUT_MS=30000
OVERLAP_SAMPLE_VALUES=1000
OVERLAP_MIN_CONFIDENCE=0.90
