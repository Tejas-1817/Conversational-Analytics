fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0.30
psycopg[binary]>=3.2        # metadata repo + postgres sources
pymysql>=1.1                # mysql sources
pydantic>=2.7
pydantic-settings>=2.3
cryptography>=42.0          # Fernet + AES-GCM credential encryption
rq>=1.16                    # async job queue
redis>=5.0
structlog>=24.1
pyjwt>=2.8
bcrypt>=4.1
google-genai>=0.2.0
tenacity>=8.3.0
asgi-correlation-id>=4.3.1
python-multipart

# Phase 6 — Security & Multi-Tenancy
slowapi>=0.1.9              # rate limiting (optional but recommended)
authlib>=1.3.0              # OIDC / SSO client
httpx>=0.27.0               # async HTTP for OIDC token exchange
